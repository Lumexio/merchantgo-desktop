name: Build & Publish Native Desktop Executables

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build-desktop:
    name: 💻 Compile Windows & Linux Executables
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          
      - name: Install & Build
        run: |
          npm ci --legacy-peer-deps
          npm run build
        env:
          VITE_API_BASE_URL: https://api.merchantgo.store
          
      - name: Compile Electron Executables
        run: npx electron-builder --linux --win -c.extraMetadata.main=build/electron.js
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
      - name: Upload to GitHub Releases
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/*.exe
            dist/*.AppImage
